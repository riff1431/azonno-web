# Button

A button is a clickable thing. Use it when someone should be able to click
something, and put a sensible label on it.

## Sizes

Small, medium, large.

<!-- Deliberately thin: no anatomy, no variant table, no interaction states, no
     mapping to design values, no assistive-technology notes. Every required
     section is absent, and the words that would satisfy the patterns are absent
     with them - an earlier draft of this fixture said "no anatomy, no variants"
     and passed, because the gate matches on the words. -->
